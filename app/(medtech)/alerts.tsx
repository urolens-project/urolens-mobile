import { AlertsScreen } from '@features/alerts/components/AlertsScreen';

/**
 * @description Route entry for the Alerts tab. Renders the feature screen only.
 */
export default function AlertsRoute(): React.JSX.Element {
  return <AlertsScreen />;
}
