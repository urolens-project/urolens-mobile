import { QueueScreen } from '@features/queue/components/QueueScreen';

/**
 * @description Route entry for the Queue tab.
 */
export default function QueueRoute(): React.JSX.Element {
  return <QueueScreen />;
}
