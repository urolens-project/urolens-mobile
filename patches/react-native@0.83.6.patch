diff --git a/src/private/webapis/dom/events/Event.js b/src/private/webapis/dom/events/Event.js
index 6f616735af8d294fd38279673830eb9de00d5f70..faa97812d19af5a343374f392322ff064a7c3421 100644
--- a/src/private/webapis/dom/events/Event.js
+++ b/src/private/webapis/dom/events/Event.js
@@ -49,10 +49,12 @@ export default class Event {
   static +AT_TARGET: 2;
   static +BUBBLING_PHASE: 3;
 
-  +NONE: 0;
-  +CAPTURING_PHASE: 1;
-  +AT_TARGET: 2;
-  +BUBBLING_PHASE: 3;
+  // Instance field declarations removed: with Babel loose:true (required by
+  // WatermelonDB decorators), these compile to `this.NONE = void 0` in the
+  // constructor which throws in strict mode because Object.defineProperty
+  // below sets Event.prototype.NONE as non-writable. Values are still
+  // correctly accessible via prototype lookup from the Object.defineProperty
+  // calls at the bottom of this file.
 
   _bubbles: boolean;
   _cancelable: boolean;
